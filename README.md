A project using the react + redax libraries, 
and navigation between pages implemented using the React router, 
data transfer to the application using the fetch function is implemented by the JSON server, 
so before starting the project on the local server, you must first run the server 'json-server src/db.json'
